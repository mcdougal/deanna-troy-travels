import { spacing } from '@lib/mui';

export default {
  navItemsContainer: {
    display: `flex`,
    flexWrap: `wrap`,
    justifyContent: `center`,
    marginX: `auto`,
    maxWidth: spacing(40),
  },
  navItemContainer: {
    marginBottom: 2,
  },
};
